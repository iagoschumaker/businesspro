import React from 'react';
import Card from '../../components/Common/Card';
import { Ban } from 'lucide-react';

const Billing: React.FC = () => {
  return (
    <div className="container mx-auto p-4">
      <Card>
        <div className="flex items-center justify-between mb-6">
          <h1 className="text-2xl font-bold">Gerenciamento Financeiro</h1>
        </div>
        
        <div className="bg-slate-100 rounded-lg p-8 text-center">
          <Ban size={64} className="mx-auto mb-4 text-slate-500" />
          <h2 className="text-xl font-semibold mb-2">Funcionalidade de Boletos Desativada</h2>
          <p className="text-slate-600 mb-4">
            Conforme solicitado, o sistema de geração e gerenciamento de boletos foi removido.
          </p>
          <p className="text-slate-500">
            Para reativar ou implementar outro sistema de pagamento, entre em contato com o administrador do sistema.
          </p>
        </div>
      </Card>
    </div>
  );
};

export default Billing;
